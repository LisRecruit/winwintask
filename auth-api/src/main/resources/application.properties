server.port=8080

spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:auth_db}
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=${JWT_SECRET:vzyKqROxhBglP8jQf/oFBz9YSuzZb+VV++ZrxMzJc/k=}
jwt.expiration=${JWT_EXPIRATION:3600000}

dataapi.url=${DATAAPI_URL:http://data-api:8081/api/transform}
dataapi.internal-token=${INTERNAL_TOKEN:myinternaltoken}